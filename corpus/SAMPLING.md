# Sampling log

Generated by fieldnotes 0.1.0 on 2026-09-12 12:55 UTC. This file describes how the accompanying corpus was drawn; it is meant to be read alongside any finding taken from it.


## What was asked for

- **Source:** hn
- **Queries:** `certificate warning`, `self-signed certificate`, `click through the warning`, `ignore the warning`, `ignored the warning`, `clicked through`, `warning fatigue`, `alert fatigue`, `phishing training`, `security prompt`, `cert error`, `just click accept`
  <br>Searched separately and merged into one corpus. A term that matched nothing still appears above, because the terms that failed are part of the search.
- **Include comments:** True
- **Min length chars:** 200
- **Pseudonymised:** True
- **Since:** 2021-01-01
- **Thread limit:** 40

Endpoints used:

- `https://hn.algolia.com/api/v1/search_by_date`
- `https://hn.algolia.com/api/v1/items/{id}`

## What came back

| Stage | Items |
| --- | ---: |
| stories matched by 'certificate warning' | 32 |
| retrieved for query 'certificate warning' | 152 |
| stories matched by 'self-signed certificate' | 22 |
| retrieved for query 'self-signed certificate' | 154 |
| stories matched by 'click through the warning' | 40 |
| retrieved for query 'click through the warning' | 408 |
| stories matched by 'ignore the warning' | 29 |
| retrieved for query 'ignore the warning' | 148 |
| stories matched by 'ignored the warning' | 40 |
| retrieved for query 'ignored the warning' | 333 |
| stories matched by 'clicked through' | 40 |
| retrieved for query 'clicked through' | 346 |
| stories matched by 'warning fatigue' | 40 |
| retrieved for query 'warning fatigue' | 251 |
| stories matched by 'alert fatigue' | 36 |
| retrieved for query 'alert fatigue' | 242 |
| stories matched by 'phishing training' | 40 |
| retrieved for query 'phishing training' | 370 |
| stories matched by 'security prompt' | 40 |
| retrieved for query 'security prompt' | 344 |
| stories matched by 'cert error' | 14 |
| retrieved for query 'cert error' | 148 |
| stories matched by 'just click accept' | 40 |
| retrieved for query 'just click accept' | 262 |
| items retrieved (posts + comments) | 3,158 |
| items included in corpus | 1,785 |

### What was excluded, and why

| Reason | Items |
| --- | ---: |
| already retrieved under an earlier query | 215 |
| comment shorter than 200 characters | 1,158 |

## The corpus

1,785 items across 372 threads — 372 posts and 1,413 comments.

Observed date range: **2021-01-11** to **2026-09-12**.

Note that this is the range of what was *returned*, which is not the same as the range that was *requested*. If they differ, the difference is itself a finding about coverage.

## What this sample cannot support

- Deleted and moderator-removed content is invisible to this method. A thread's most contested contributions are the most likely to be missing.
- Posters are not users. The overwhelming majority of people who read a forum never post, and nothing here represents them.
- Visibility is ranked, not neutral. What a search returns is shaped by the platform's own relevance and score ordering, which correlates with age and engagement rather than with representativeness.
- Self-selection: people post about an experience when it is unusual, unresolved or annoying. Base rates cannot be estimated from this data.
- Identity is unverified. Claimed roles, employers and seniority are claims.
- Hacker News is a single community with a specific demographic — largely English-speaking, technical and US-weighted. Nothing here generalises beyond it.
- The thread limit (40) was reached for 'click through the warning', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'click through the warning' returned is 2025-01-14. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'ignored the warning', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'ignored the warning' returned is 2026-01-27. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'clicked through', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'clicked through' returned is 2025-01-16. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'warning fatigue', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'warning fatigue' returned is 2024-08-11. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'phishing training', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'phishing training' returned is 2024-06-17. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'security prompt', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'security prompt' returned is 2026-07-15. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (40) was reached for 'just click accept', so that query's contribution is truncated: it is the most recent 40 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'just click accept' returned is 2022-07-09. The gap is a truncation artefact, not evidence that nothing was posted in between.

## Reproducing it

```sh
python3 fieldnotes.py hn "certificate warning" "self-signed certificate" "click through the warning" "ignore the warning" "ignored the warning" "clicked through" "warning fatigue" "alert fatigue" "phishing training" "security prompt" "cert error" "just click accept" --since 2021-01-01 --min-length 200 --limit 40 --out corpus/
```

Re-running will not necessarily return the same corpus. Threads are edited, deleted and re-ranked, so a later run is a new sample, not a verification of this one. Treat the CSV as the artifact of record.
