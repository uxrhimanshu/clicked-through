# Sampling log

Generated by fieldnotes 0.1.0 on 2026-09-12 12:37 UTC. This file describes how the accompanying corpus was drawn; it is meant to be read alongside any finding taken from it.


## What was asked for

- **Source:** hn
- **Queries:** `certificate warning`, `certificate error`, `self-signed certificate`, `security warning`, `browser warning`, `cert warning`, `SSL warning`, `click through the warning`, `ignore the warning`, `ignored the warning`, `dismiss the warning`, `bypass the warning`, `clicked through`, `warning fatigue`, `alert fatigue`, `phishing training`, `security prompt`
  <br>Searched separately and merged into one corpus. A term that matched nothing still appears above, because the terms that failed are part of the search.
- **Include comments:** True
- **Items must match a query term:** True
- **Min length chars:** 120
- **Pseudonymised:** True
- **Since:** 2021-01-01
- **Thread limit:** 60

Endpoints used:

- `https://hn.algolia.com/api/v1/search_by_date`
- `https://hn.algolia.com/api/v1/items/{id}`

## What came back

| Stage | Items |
| --- | ---: |
| stories matched by 'certificate warning' | 60 |
| retrieved for query 'certificate warning' | 408 |
| stories matched by 'certificate error' | 55 |
| retrieved for query 'certificate error' | 1,322 |
| stories matched by 'self-signed certificate' | 23 |
| retrieved for query 'self-signed certificate' | 201 |
| stories matched by 'security warning' | 60 |
| retrieved for query 'security warning' | 825 |
| stories matched by 'browser warning' | 60 |
| retrieved for query 'browser warning' | 399 |
| stories matched by 'cert warning' | 52 |
| retrieved for query 'cert warning' | 940 |
| stories matched by 'SSL warning' | 20 |
| retrieved for query 'SSL warning' | 111 |
| stories matched by 'click through the warning' | 60 |
| retrieved for query 'click through the warning' | 773 |
| stories matched by 'ignore the warning' | 60 |
| retrieved for query 'ignore the warning' | 181 |
| stories matched by 'ignored the warning' | 60 |
| retrieved for query 'ignored the warning' | 457 |
| stories matched by 'dismiss the warning' | 4 |
| retrieved for query 'dismiss the warning' | 11 |
| stories matched by 'bypass the warning' | 60 |
| retrieved for query 'bypass the warning' | 614 |
| stories matched by 'clicked through' | 60 |
| retrieved for query 'clicked through' | 601 |
| stories matched by 'warning fatigue' | 2 |
| retrieved for query 'warning fatigue' | 17 |
| stories matched by 'alert fatigue' | 36 |
| retrieved for query 'alert fatigue' | 242 |
| stories matched by 'phishing training' | 51 |
| retrieved for query 'phishing training' | 422 |
| stories matched by 'security prompt' | 60 |
| retrieved for query 'security prompt' | 1,028 |
| items retrieved (posts + comments) | 8,552 |
| items included in corpus | 103 |

### What was excluded, and why

| Reason | Items |
| --- | ---: |
| already retrieved under an earlier query | 1,094 |
| does not mention any query term | 7,355 |

## The corpus

103 items across 77 threads — 73 posts and 30 comments.

Observed date range: **2021-01-11** to **2026-09-04**.

Note that this is the range of what was *returned*, which is not the same as the range that was *requested*. If they differ, the difference is itself a finding about coverage.

## What this sample cannot support

- Deleted and moderator-removed content is invisible to this method. A thread's most contested contributions are the most likely to be missing.
- Posters are not users. The overwhelming majority of people who read a forum never post, and nothing here represents them.
- Visibility is ranked, not neutral. What a search returns is shaped by the platform's own relevance and score ordering, which correlates with age and engagement rather than with representativeness.
- Self-selection: people post about an experience when it is unusual, unresolved or annoying. Base rates cannot be estimated from this data.
- Identity is unverified. Claimed roles, employers and seniority are claims.
- Hacker News is a single community with a specific demographic — largely English-speaking, technical and US-weighted. Nothing here generalises beyond it.
- The thread limit (60) was reached for 'certificate warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'certificate warning' returned is 2024-11-19. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'security warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'security warning' returned is 2026-06-28. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'browser warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'browser warning' returned is 2024-12-16. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'click through the warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'click through the warning' returned is 2025-11-02. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'ignore the warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'ignore the warning' returned is 2024-06-10. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'ignored the warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'ignored the warning' returned is 2025-12-17. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'bypass the warning', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'bypass the warning' returned is 2024-02-01. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'clicked through', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'clicked through' returned is 2026-04-22. The gap is a truncation artefact, not evidence that nothing was posted in between.
- The thread limit (60) was reached for 'security prompt', so that query's contribution is truncated: it is the most recent 60 matching threads, not all of them. Raise --limit or narrow the query before treating this as complete coverage.
- Requested items from 2021-01-01 onward, but the earliest item 'security prompt' returned is 2026-06-13. The gap is a truncation artefact, not evidence that nothing was posted in between.

## Reproducing it

```sh
python3 fieldnotes.py hn "certificate warning" "certificate error" "self-signed certificate" "security warning" "browser warning" "cert warning" "SSL warning" "click through the warning" "ignore the warning" "ignored the warning" "dismiss the warning" "bypass the warning" "clicked through" "warning fatigue" "alert fatigue" "phishing training" "security prompt" --since 2021-01-01 --min-length 120 --limit 60 --items-must-match --out corpus-lexical-screen/
```

Re-running will not necessarily return the same corpus. Threads are edited, deleted and re-ranked, so a later run is a new sample, not a verification of this one. Treat the CSV as the artifact of record.
